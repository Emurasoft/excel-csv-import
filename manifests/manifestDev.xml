<?xml version="1.0" encoding="UTF-8"?>
<OfficeApp
    xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
    xmlns:ov="http://schemas.microsoft.com/office/taskpaneappversionoverrides"
    xsi:type="TaskPaneApp"
>
    <Id>5ba168da-3995-456c-8770-f8b4a499c617</Id>

    <!--Version. Updates from the store only get triggered if there is a version change. -->
    <Version>1.0.0</Version>
    <ProviderName>Emurasoft, Inc.</ProviderName>
    <DefaultLocale>en-US</DefaultLocale>
    <!-- The display name of your add-in. Used on the store and various places of the Office UI such as the add-ins dialog. -->
    <DisplayName DefaultValue="CSV Import+Export" />
    <Description DefaultValue="[Workbook Add-in description]" /> <!--TODO-->

    <!-- Icon for your add-in. Used on installation screens and the add-ins dialog. -->
    <IconUrl DefaultValue="https://localhost:3000/icon-32.png" />
    <HighResolutionIconUrl DefaultValue="https://localhost:3000/hi-res-icon.png" />

    <SupportUrl DefaultValue="https://www.emeditor.com/support/#contact" /><!--TODO-->

    <!--Begin TaskPane Mode integration. This section is used if there are no VersionOverrides or if the Office client version does not support add-in commands. -->
    <Hosts>
        <Host Name="Workbook" />
    </Hosts>
    <DefaultSettings>
        <SourceLocation DefaultValue="https://localhost:3000/index.html" />
    </DefaultSettings>
    <!-- End TaskPane Mode integration.  -->

    <Permissions>ReadWriteDocument</Permissions>

    <!-- Begin Add-in Commands Mode integration. -->
    <VersionOverrides xmlns="http://schemas.microsoft.com/office/taskpaneappversionoverrides" xsi:type="VersionOverridesV1_0">

        <!-- The Hosts node is required. -->
        <Hosts>
            <!-- Each host can have a different set of commands. -->
            <!-- Excel host is Workbook, Word host is Document, and PowerPoint host is Presentation. -->
            <!-- Make sure the hosts you override match the hosts declared in the top section of the manifest. -->
            <Host xsi:type="Workbook">
                <!-- Form factor. Currently only DesktopFormFactor is supported. -->
                <DesktopFormFactor>
                    <!-- Function file is a HTML page that includes the JavaScript where functions for ExecuteAction will be called.
                      Think of the FunctionFile as the code behind ExecuteFunction. -->
                    <FunctionFile resid="mainURL" />

                    <!-- PrimaryCommandSurface is the main Office Ribbon. -->
                    <ExtensionPoint xsi:type="PrimaryCommandSurface">
                        <!-- Use OfficeTab to extend an existing Tab. Use CustomTab to create a new tab. -->
                        <OfficeTab id="TabInsert">
                            <!-- Ensure you provide a unique id for the group. Recommendation for any IDs is to namespace using your company name. -->
                            <Group id="Emurasoft.Group">
                                <!-- Label for your group. resid must point to a ShortString resource. -->
                                <Label resid="Emurasoft.GroupLabel" />
                                <!-- Icons. Required sizes 16,32,80, optional 20, 24, 40, 48, 64. Strongly recommended to provide all sizes for great UX. -->
                                <!-- Use PNG icons. All URLs on the resources section must use HTTPS. -->
                                <Icon>
                                    <bt:Image size="16" resid="Emurasoft.icon16" />
                                    <bt:Image size="32" resid="Emurasoft.icon32" />
                                    <bt:Image size="80" resid="Emurasoft.icon80" />
                                </Icon>

                                <!-- Control. It can be of type "Button" or "Menu". -->
                                <Control xsi:type="Button" id="Emurasoft.Button">
                                    <Label resid="Emurasoft.Button.Label" />
                                    <Supertip>
                                        <Title resid="Emurasoft.Button.Title" />
                                        <Description resid="Emurasoft.Button.Description" />
                                    </Supertip>
                                    <Icon>
                                        <bt:Image size="16" resid="Emurasoft.icon16" />
                                        <bt:Image size="32" resid="Emurasoft.icon32" />
                                        <bt:Image size="80" resid="Emurasoft.icon80" />
                                    </Icon>

                                    <!-- This is what happens when the command is triggered (E.g. click on the Ribbon). Supported actions are ExecuteFunction or ShowTaskpane. -->
                                    <Action xsi:type="ShowTaskpane">
                                        <TaskpaneId>Taskpane1</TaskpaneId>
                                        <!-- Provide a url resource id for the location that will be displayed on the task pane. -->
                                        <SourceLocation resid="Emurasoft.mainURL" />
                                    </Action>
                                </Control>
                            </Group>
                        </OfficeTab>
                    </ExtensionPoint>
                </DesktopFormFactor>
            </Host>
        </Hosts>

        <!-- You can use resources across hosts and form factors. -->
        <Resources>
            <bt:Images>
                <bt:Image id="Emurasoft.icon16" DefaultValue="https://localhost:3000/icon-16.png" />
                <bt:Image id="Emurasoft.icon32" DefaultValue="https://localhost:3000/icon-32.png" />
                <bt:Image id="Emurasoft.icon80" DefaultValue="https://localhost:3000/icon-80.png" />
            </bt:Images>
            <bt:Urls>
                <bt:Url id="Emurasoft.mainURL" DefaultValue="https://localhost:3000/index.html" />
            </bt:Urls>
            <!-- ShortStrings max characters==125. -->
            <bt:LongStrings>
                <bt:String id="Emurasoft.Button.Label" DefaultValue="CSV Import+Export" />
                <bt:String id="Emurasoft.GroupLabel" DefaultValue="CSV" />
                <bt:String id="Emurasoft.Button.Title" DefaultValue="CSV Import+Export" />
                <bt:String id="Emurasoft.Button.Description" DefaultValue="Import CSV" />
            </bt:LongStrings>
        </Resources>
    </VersionOverrides>
    <!-- End Add-in Commands Mode integration. -->
</OfficeApp>
